<Window x:Class="ProjektTechniki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektTechniki"
        mc:Ignorable="d"
        Title="MainWindow" Height="1000" Width="1000"
        DataContext="{Binding Main, Source={StaticResource Locator}}">
    <Grid>
        <Button Height="50" Width="50" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,820,0" Content="{Binding Text, Mode=TwoWay}"></Button>  
    </Grid>
</Window>
