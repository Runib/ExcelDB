<Window x:Class="ProjektTechniki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektTechniki"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000"
        DataContext="{Binding Main, Source={StaticResource Locator}}">
    <Grid Background="Lavender">
        <DockPanel>
            <Menu Width="auto" Height="20" Background="#FFA9D1F4" DockPanel.Dock="Top"
                  VerticalAlignment="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_New"/>
                    <MenuItem Header="_Open"/>
                    <Separator />
                    <MenuItem Header="_Exit" />
                </MenuItem>
            </Menu>
        </DockPanel>

        <TextBox Margin="30 30 0 0" Height="32" VerticalAlignment="Top" Width="100"
                 HorizontalAlignment="Left" Name="AddBox" />
    
        <Button Content="Dodaj" Height="32" HorizontalAlignment="Left" Margin="150,30,0,0" 
                Name="button1" VerticalAlignment="Top" Width="88" Click="button1_Click" 
                Background="White" >
            
        </Button>
        








    </Grid>
</Window>
